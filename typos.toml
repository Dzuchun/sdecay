[files]
extend-exclude = [
    # "target/", # not a code
    "sys/vendor", # not my code
    "sys/bindings.rs", # generated from not my code
]
ignore-hidden = true

[type.rust]
extend-ignore-identifiers-re = [
    "Ba",
    "ba", 
    "Nd",
    "nd",
]
